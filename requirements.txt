# ── Existing (unchanged) ─────────────────────────────────────────────────────
groq==0.11.0
httpx==0.27.0
chromadb==0.5.5
sentence-transformers==3.0.1
pdfplumber==0.11.0
python-docx==1.1.2
streamlit==1.36.0
python-dotenv==1.0.1
pandas==2.2.2
numpy==1.26.4

# ── Auth ─────────────────────────────────────────────────────────────────────
bcrypt==4.1.3

# ── Text-to-Speech ───────────────────────────────────────────────────────────
gTTS==2.5.1

# ── Speech-to-Text ───────────────────────────────────────────────────────────
SpeechRecognition==3.10.4
# PyAudio==0.2.14   # Uncomment if microphone input works on your system
#                   # Windows: pip install pipwin && pipwin install pyaudio
#                   # Linux:   sudo apt install portaudio19-dev && pip install pyaudio
#                   # macOS:   brew install portaudio && pip install pyaudio

# Voice input uses browser Web Speech API — no PyAudio required
